# SIH26001 — example environment file.
# Copy to `.env` (git-ignored) and fill in real local values.
# No real secrets belong in this file or in git.

# --- PostgreSQL / PostGIS ---
POSTGRES_DB=sih26001_dev
POSTGRES_USER=change_me
POSTGRES_PASSWORD=change_me
POSTGRES_PORT=5432

# --- Service ports (local dev defaults, not final) ---
ML_SERVICE_PORT=8000
BACKEND_PORT=8080
FRONTEND_PORT=3000

# --- Alerting providers (placeholders — no provider selected yet, see
#     docs/architecture.md §10 Open Architectural Questions) ---
# SMS_PROVIDER_API_KEY=
# VOICE_PROVIDER_API_KEY=
